server:
  port: 8888
spring:
  application:
    name: ecom-config-server
  profiles:
    active: local
  cloud:
    config:
      server:
        git:
          uri: https://github.com/duynnapt/ecom-configure
          clone-on-start: true
  security:
    user:
      name: root
      password: admin
